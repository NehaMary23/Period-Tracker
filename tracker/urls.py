from django.urls import path
from . import views

app_name = 'tracker'

# API endpoints (to be implemented with proper serializers)
urlpatterns = [
    # These are placeholder paths for future API implementation
    # Currently, proper REST API views need to be created
]
